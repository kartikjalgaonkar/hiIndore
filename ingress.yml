apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: hi-indore
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /
spec:
    rules:
        -
            http: {paths: [{path: /hello, backend: {serviceName: hi-indore, servicePort: 8081}}]}
